module BankingData
  class Bank

    # goal
    # BankingData::Bank.where(locale: :de).only(:bic)
    #def self.where(options = {})
      #locale = options.delete(:locale)
      #if locale = 'DE'
        #Countries::De.where(options)
      #end
    #end
  end
end
